特征是这次写的格式十分整齐（似乎无意义），很多地方使用了<<和(UINTX_T)来简化操作。
使用了UNROLL和PIPLINE优化总体效率应该还可以。

【重要】
我用了一中特殊的结构来实现AES.
一般而言AES每一组state都是一个元素，但是我这次使用了uint32_t作为基本单位，也就是每4个元素存储为一个单位。
虽然因此增加指针移动次数，但是已经被我优化为位移和类型转化操作，且每次转化操作不超过3个基本指令。
相应的消除了3/4的访存，理论上有一定的加速效果。

效率大约为16ms/1w次